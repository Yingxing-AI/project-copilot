# v0.3.1

## Changes since v0.3.0

- Stopped active workflows from writing new decisions into legacy `DECISIONS.md`.
- Moved `continue_development` to the Session-first path and reduced legacy `HYPOTHESES.md` exposure in the main flow.
- Wrote initial confirmed decisions to ADR during project initialization, with duplicate protection for repeated init runs.
- Split inline `初始 Decisions` lists on `；` / `;` so multiple initial decisions generate separate ADRs.
- Removed stale `暂无 ADR。` placeholders from ADR index once real ADR entries exist.
- Fixed Validation README Drift false positives caused by freshly generated validation artifacts.
- Synced README, architecture docs, status, validation outputs, and release notes to the current stable surface.

## Stable Scope

- Project Copilot remains a local-first project memory layer for Codex.
- Charter, ADR, Session Archive, Memory Health, and Validation remain the stable core surfaces.
- Legacy files stay in compatibility or legacy state, but active workflows no longer treat them as primary write targets.

## Verified

- `pytest -q`
- Current baseline: 61 passed.
