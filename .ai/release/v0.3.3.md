# v0.3.3

## Changes since v0.3.2

- Updated `close_day` so confirmed end-of-day work now synchronizes `validation.json`, `derived/metrics.json`, and `docs/validation-report.md` together.
- Added shared validation artifact synchronization to keep end-of-day and manual refresh behavior aligned.
- Added regression coverage for the close-day validation refresh path.
- Synced the Codex workflow docs and dashboard-facing guidance to the new three-file refresh rule.

## Stable Scope

- Project Copilot remains a local-first memory layer for Codex.
- Validation remains derived from real `.ai` state and does not become a primary memory source.
- End-of-day workflows now guarantee the validation snapshot, derived metrics, and report stay aligned.

## Verified

- `pytest -q`
- Current baseline: 65 passed.
